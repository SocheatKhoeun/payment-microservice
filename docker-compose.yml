version: '3.8'

services:
	payment:
		image: docker.io/socheatsama/payment-microservice:latest
		build:
			context: .
			dockerfile: Dockerfile
		ports:
			- "3000:3000"
		environment:
			- PORT=3000
			- NODE_ENV=production
		restart: unless-stopped

